<% provide(:title, 'Добавление компании') %>
<h1>Добавление компании</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@company) do |f| %>
       <%= render 'fields', f: f %>
      <%= f.submit "Create", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
