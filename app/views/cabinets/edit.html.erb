<% provide(:title, "Редактирование кабинета") %>
<h1>Редактирование кабинета</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@cabinet) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Сохранить изменения", class: "btn btn-large btn-primary" %>
    <% end %>

  </div>
</div>
