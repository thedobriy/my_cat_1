<% provide(:title, @doctor.name) %>
<div class="row">
      <h1>
        <%= @doctor.name %>
      </h1>
      <p><%= @doctor.desc %>
      <% if @doctor.company %>
      <p><%= link_to @doctor.company.name, @doctor.company %>
      <% end %>
</div>

